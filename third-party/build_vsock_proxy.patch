diff --git a/Cargo.toml b/Cargo.toml
index b527046..23e1f8a 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -39,3 +39,5 @@ strip = true
 lto = true
 panic = "abort"
 codegen-units = 1
+
+[workspace]
diff --git a/src/ip_to_vsock_transparent.rs b/src/ip_to_vsock_transparent.rs
index 7c1b3af..0de376f 100644
--- a/src/ip_to_vsock_transparent.rs
+++ b/src/ip_to_vsock_transparent.rs
@@ -50,7 +50,7 @@ struct Cli {

 pub async fn proxy(listen_addr: &str, server_addr: VsockAddr) -> Result<()> {
     println!("Listening on: {}", listen_addr);
-    println!("Proxying to: {:?}", server_addr);
+    // println!("Proxying to: {:?}", server_addr);

     let listener = TcpListener::bind(listen_addr).await?;

@@ -76,7 +76,7 @@ async fn transfer(mut inbound: TcpStream, proxy_addr: VsockAddr) -> Result<()> {
     let orig_dst = inbound
         .get_original_dst()
         .ok_or(anyhow!("Failed to retrieve original destination"))?;
-    println!("Original destination: {}", orig_dst);
+    // println!("Original destination: {}", orig_dst);

     let mut outbound = VsockStream::connect(proxy_addr)
         .await
@@ -99,7 +99,7 @@ async fn transfer(mut inbound: TcpStream, proxy_addr: VsockAddr) -> Result<()> {
             .await
             .context("error in ip to vsock copy")
             .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e))?;
-        println!("ip to vsock copy exited {}", orig_dst);
+        // println!("ip to vsock copy exited {}", orig_dst);
         wo.shutdown().await
     };

@@ -108,7 +108,7 @@ async fn transfer(mut inbound: TcpStream, proxy_addr: VsockAddr) -> Result<()> {
             .await
             .context("error in vsock to ip copy")
             .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e))?;
-        println!("vsock to ip copy exited {}", orig_dst);
+        // println!("vsock to ip copy exited {}", orig_dst);
         wi.shutdown().await
     };

diff --git a/src/vsock_to_ip_transparent.rs b/src/vsock_to_ip_transparent.rs
index ab58d7c..c13f31e 100644
--- a/src/vsock_to_ip_transparent.rs
+++ b/src/vsock_to_ip_transparent.rs
@@ -77,7 +77,7 @@ async fn transfer(mut inbound: VsockStream) -> Result<()> {
         IpAddr::V4(ri.read_u32_le().await?.into()),
         ri.read_u16_le().await?,
     );
-    println!("Proxying to: {:?}", proxy_addr);
+    // println!("Proxying to: {:?}", proxy_addr);

     let mut outbound = TcpStream::connect(proxy_addr)
         .await
@@ -90,7 +90,7 @@ async fn transfer(mut inbound: VsockStream) -> Result<()> {
             .await
             .context("error in vsock to ip copy")
             .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e))?;
-        println!("vsock to ip copy exited {}", proxy_addr);
+        // println!("vsock to ip copy exited {}", proxy_addr);
         wo.shutdown().await
     };

@@ -99,7 +99,7 @@ async fn transfer(mut inbound: VsockStream) -> Result<()> {
             .await
             .context("error in ip to vsock copy")
             .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e))?;
-        println!("ip to vsock copy exited {}", proxy_addr);
+        // println!("ip to vsock copy exited {}", proxy_addr);
         wi.shutdown().await
     };

